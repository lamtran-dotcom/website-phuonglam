name: FTP deploy disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Netlify handles deploys
    runs-on: ubuntu-latest
    steps:
      - name: Explain current deploy workflow
        run: |
          echo "FTP deploy is disabled."
          echo "The website is deployed automatically by Netlify when main is pushed."
          echo "Live site: https://phuonglam.com"
